import classes from "./LoginHead.module.scss";

function RegisterHeader() {
  return (
    <div>
      <title className={classes.loginTitle}>Register</title>
      <div>Please Register with Your Information</div>
    </div>
  );
}

export default RegisterHeader;
